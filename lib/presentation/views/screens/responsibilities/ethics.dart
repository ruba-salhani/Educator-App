import 'package:flutter/material.dart';

class Ethics extends StatelessWidget {
  const Ethics({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Responsibilities of education'),
      ),
    );
  }
}
